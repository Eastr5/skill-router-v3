\documentclass[tikz, border=12pt]{standalone}
\usepackage{tikz}
\usepackage{helvet}
\renewcommand{\familydefault}{\sfdefault}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, calc, fit, backgrounds}

% Okabe-Ito colorblind-safe palette
\definecolor{mcpblue}{HTML}{0072B2}
\definecolor{skillorange}{HTML}{D55E00}
\definecolor{toolgreen}{HTML}{009E73}
\definecolor{darkgray}{HTML}{333333}
\definecolor{lightgray}{HTML}{F5F5F5}
\definecolor{medgray}{HTML}{999999}

\begin{document}
\begin{tikzpicture}[
    node distance=1.2cm and 1.5cm,
    >=Stealth,
    thick,
    box/.style={
        rectangle, rounded corners=4pt, minimum width=3.2cm, minimum height=1.0cm,
        text centered, font=\small\bfseries, draw=darkgray, line width=0.8pt
    },
    layerbox/.style={
        rectangle, rounded corners=3pt, minimum width=3.0cm, minimum height=2.8cm,
        draw=darkgray, line width=0.6pt, fill=lightgray
    },
    layerlabel/.style={
        font=\footnotesize\bfseries, text centered
    },
    item/.style={
        font=\scriptsize, text centered, text width=2.6cm
    },
    decision/.style={
        rectangle, rounded corners=3pt, minimum width=2.4cm, minimum height=0.7cm,
        text centered, font=\scriptsize, draw=darkgray, line width=0.5pt,
        fill=white
    },
    arrow/.style={
        ->, line width=1.2pt, darkgray
    },
    smallarrow/.style={
        ->, line width=0.8pt, darkgray
    }
]

% Top: User Request
\node[box, fill=white, minimum width=6cm] (user) {USER REQUEST};

% Intent Analysis box
\node[box, fill=white, minimum width=6cm, below=0.8cm of user] (intent) {INTENT ANALYSIS};
\node[font=\tiny, text=medgray, below=0.05cm of intent] (intentdetail) {Task type · Complexity · Urgency · Constraints};

% Three layers
\node[layerbox, below=1.0cm of intent, xshift=-3.6cm, fill=mcpblue!8] (mcp) {};
\node[layerlabel, above=0.15cm of mcp.center, text=mcpblue] {MCP Layer};
\node[item, below=0.3cm of mcp.center, text=darkgray] {External Services\\paper-search · MySQL · GitHub};

\node[layerbox, below=1.0cm of intent, fill=skillorange!8] (skill) {};
\node[layerlabel, above=0.15cm of skill.center, text=skillorange] {Skill Layer};
\node[item, below=0.3cm of skill.center, text=darkgray] {Domain Knowledge\\TikZ · Code Review · Data};

\node[layerbox, below=1.0cm of intent, xshift=3.6cm, fill=toolgreen!8] (tool) {};
\node[layerlabel, above=0.15cm of tool.center, text=toolgreen] {Tool Layer};
\node[item, below=0.3cm of tool.center, text=darkgray] {Native IDE Ops\\Read · RunCommand · Task};

% Arrows from intent to layers
\draw[arrow] (intent.south -| mcp.north) -- (mcp.north);
\draw[arrow] (intent.south -| skill.north) -- (skill.north);
\draw[arrow] (intent.south -| tool.north) -- (tool.north);

% Bottom: Execution Decision
\node[box, fill=white, minimum width=6cm, below=1.0cm of skill] (exec) {EXECUTION DECISION};

% Arrows from layers to exec
\draw[arrow] (mcp.south) -- (mcp.south |- exec.north);
\draw[arrow] (skill.south) -- (exec.north);
\draw[arrow] (tool.south) -- (tool.south |- exec.north);

% Decision branches
\node[decision, below=0.6cm of exec, xshift=-2.2cm, fill=mcpblue!10] (auto) {Auto-Invoke};
\node[decision, below=0.6cm of exec, fill=skillorange!10] (rec) {Recommend};
\node[decision, below=0.6cm of exec, xshift=2.2cm, fill=toolgreen!10] (inter) {Interview};

% Confidence labels
\node[font=\tiny, text=medgray, above=0.05cm of auto] {≥ 0.9};
\node[font=\tiny, text=medgray, above=0.05cm of rec] {0.7–0.9};
\node[font=\tiny, text=medgray, above=0.05cm of inter] {< 0.7};

% Arrows to decisions
\draw[smallarrow] (exec.south -| auto.north) -- (auto.north);
\draw[smallarrow] (exec.south -| rec.north) -- (rec.north);
\draw[smallarrow] (exec.south -| inter.north) -- (inter.north);

% Title
\node[font=\large\bfseries, above=0.6cm of user, text=darkgray] {MST (MCP–Skill–Tool) Architecture};

\end{tikzpicture}
\end{document}
